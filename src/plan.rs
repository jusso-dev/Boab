//! Plan generation. Implemented in Phase 4.

use anyhow::Result;

use crate::model::asset::TargetMilestone;
use crate::model::plan::Plan;

pub fn generate(_milestone: TargetMilestone, _name: Option<String>) -> Result<Plan> {
    unimplemented!("plan generation is implemented in Phase 4")
}
